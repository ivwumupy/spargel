open std

// Warning:
//   One cannot write `1+2`!!!
//   Because `+2` will be parsed as an identifier.
// 
func main (argc: 1 + f(h, 1+ 3) + 4, argv: 1.2 + (3 + 2)) {
    let x = 1;
    return x;
}

func foo(x)
    x + 1

func xyz() {}
